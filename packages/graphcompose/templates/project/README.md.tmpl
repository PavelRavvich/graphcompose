# {{title}}

A [GraphCompose](https://github.com/PavelRavvich/graphcompose) project, generated by `gc create`.

```bash
cp .env.example .env    # your OPENROUTER_API_KEY
npm run chat            # talk to the {{kebab}} workflow
npm run describe        # its agents, tools and settings
npm run check           # types and tests
```

Add parts: `npx gc g agent|tool|mcp|rag <name> --workflow src/{{kebab}}/{{kebab}}.workflow.ts` · `npx gc help`
