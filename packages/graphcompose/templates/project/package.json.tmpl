{
  "name": "{{kebab}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
{{scripts}}
    "check": "tsc --noEmit && vitest run"
  },
  "dependencies": {
{{dependencies}}
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "{{typescriptVersion}}",
    "vitest": "^3.0.0"
  }
}
