import { Workflow } from "graphcompose";
{{imports}}

/** {{title}} — generated by `gc create`; every setting below is yours to change. */
@Workflow({
  name: "{{kebab}}",
  version: "0.1.0",
  defaults: {
    // kimi-k2.6 via the provider Inceptron loops on tool-call arguments — let OpenRouter use the others
    chat: { temperature: 0, thinking: "default", cache: true, provider: { ignore: ["Inceptron"] } },
    router: { kind: "jev", model: "typesafe/jev-1.13" },
    tools: { maxToolCalls: 8 },
    history: { limit: 8 },
  },
  budget: { runBudgetCap: 0.1, dailyBudgetCap: 2, evalBudgetCap: 1 },
  routers: { main: { maxHops: 3 } },
  guards: {
    input: { prompt_injection: { threshold: 0.7, refusal: "I can't help with that request." } },
    output: { pii: { threshold: 0.7, refusal: "The answer contained personal data and was withheld." } },
  },
  agents: [{{agents}}],
  mcp: [{{servers}}],
})
export class {{pascal}}Workflow {}
