import { Tool, type ToolHandler } from "graphcompose";
import { z } from "zod";

export const {{pascal}}Query = z.object({ query: z.string().describe("what to look up") });
export type {{pascal}}Query = z.infer<typeof {{pascal}}Query>;
export const {{pascal}}Result = z.object({ result: z.string() });
export type {{pascal}}Result = z.infer<typeof {{pascal}}Result>;

/** {{title}}. TODO: implement — it returns a placeholder so the workflow runs end to end. */
@Tool({
  name: "{{snake}}",
  description: "{{title}} (TODO: say what it does)",
  input: {{pascal}}Query,
  output: {{pascal}}Result,
})
export class {{pascal}}Tool implements ToolHandler<{{pascal}}Query, {{pascal}}Result> {
  run({ query }: {{pascal}}Query): Promise<{{pascal}}Result> {
    // TODO: the real work — dependencies come through the constructor (and `deps`)
    return Promise.resolve({ result: `TODO: {{snake}} is not implemented yet (query: ${query})` });
  }
}
