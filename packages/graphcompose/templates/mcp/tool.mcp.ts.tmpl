import { McpTool, type ToolHandler } from "graphcompose";
import { {{input}}, {{output}}, {{serverPascal}}Server } from "./{{serverKebab}}.server.js";

/** {{title}} — calls the server's "{{serverTool}}"; shape the input and the result here. */
@McpTool({
  server: {{serverPascal}}Server,
  name: "{{snake}}",
  description: "{{description}}",
  input: {{input}},
  output: {{output}},
  deps: [{{serverPascal}}Server],
})
export class {{pascal}}Tool implements ToolHandler<{{input}}, {{output}}> {
  constructor(private readonly server: {{serverPascal}}Server) {}

  run(input: {{input}}): Promise<{{output}}> {
    return this.server.call("{{serverTool}}", input);
  }
}
